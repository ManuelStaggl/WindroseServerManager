---
phase: 08-windroseplus-bootstrap
plan: 03
type: execute
wave: 2
depends_on: [08-02]
files_modified:
  - src/WindroseServerManager.App/App.axaml.cs
  - src/WindroseServerManager.Core/Services/ServerProcessService.cs
  - src/WindroseServerManager.Core/Services/IServerProcessService.cs
  - src/WindroseServerManager.App/Views/Dialogs/AboutDialog.axaml
  - src/WindroseServerManager.App/Views/Dialogs/AboutDialog.axaml.cs
  - src/WindroseServerManager.App/Resources/Licenses/WindrosePlus-LICENSE.txt
  - src/WindroseServerManager.App/WindroseServerManager.App.csproj
  - src/WindroseServerManager.App/Resources/Strings.de.axaml
  - src/WindroseServerManager.App/Resources/Strings.en.axaml
  - tests/WindroseServerManager.Core.Tests/Services/ServerProcessServiceLauncherTests.cs
autonomous: false
requirements: [WPLUS-03, WPLUS-04]

must_haves:
  truths:
    - "DI container resolves IWindrosePlusService as a singleton"
    - "ServerProcessService.StartAsync queries IWindrosePlusService.ResolveLauncher instead of hardcoding FindServerBinary"
    - "About dialog shows a 'Third-Party Licenses' section containing the WindrosePlus MIT text and a link to https://github.com/HumanGenome/WindrosePlus"
    - "The product name 'WindrosePlus' appears verbatim in the About dialog (not rebranded)"
    - "German and English localization keys exist for About.ThirdPartyLicenses.Heading, About.ThirdPartyLicenses.WindrosePlus.Intro, About.WindrosePlus.RepoLink, Warning.WindrosePlusBatMissing, Error.WindrosePlusOfflineFirstInstall"
  artifacts:
    - path: "src/WindroseServerManager.App/Resources/Licenses/WindrosePlus-LICENSE.txt"
      provides: "MIT LICENSE text embedded as app resource"
      contains: "MIT License"
    - path: "src/WindroseServerManager.App/App.axaml.cs"
      provides: "DI registration for IWindrosePlusService"
      contains: "AddSingleton<IWindrosePlusService"
    - path: "src/WindroseServerManager.Core/Services/ServerProcessService.cs"
      provides: "Launcher resolution via IWindrosePlusService"
      contains: "ResolveLauncher"
  key_links:
    - from: "ServerProcessService.StartAsync"
      to: "IWindrosePlusService.ResolveLauncher"
      via: "constructor-injected singleton"
      pattern: "_windrosePlus.ResolveLauncher\\("
    - from: "AboutDialog.axaml"
      to: "Embedded WindrosePlus-LICENSE.txt"
      via: "avares:// resource loaded at dialog open"
      pattern: "WindrosePlus-LICENSE\\.txt"
---

<objective>
Integrate `WindrosePlusService` (from Plan 02) into the running app: register in DI, inject into `ServerProcessService` so `StartAsync` uses the right launcher (WPLUS-04), embed WindrosePlus MIT LICENSE as an app resource and render it in the About dialog's new "Third-Party Licenses" section (WPLUS-03), add bilingual strings, and add xunit coverage for the ServerProcessService launcher-resolution integration.

Purpose: Close Phase 8 with a shipping-ready foundation that Phases 9-12 can consume.
Output: App builds + launches; About dialog shows WindrosePlus MIT text + link; server launcher automatically switches based on per-server WindrosePlusActive flag.
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-windroseplus-bootstrap/08-CONTEXT.md
@.planning/phases/08-windroseplus-bootstrap/08-02-SUMMARY.md
@src/WindroseServerManager.App/App.axaml.cs
@src/WindroseServerManager.Core/Services/ServerProcessService.cs
@src/WindroseServerManager.Core/Services/IServerProcessService.cs
@src/WindroseServerManager.Core/Services/WindrosePlusService.cs
@src/WindroseServerManager.App/Views/Dialogs/AboutDialog.axaml
@src/WindroseServerManager.App/Views/Dialogs/AboutDialog.axaml.cs
@src/WindroseServerManager.App/Services/LocalizationService.cs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DI registration + ServerProcessService launcher indirection + integration tests</name>
  <files>src/WindroseServerManager.App/App.axaml.cs, src/WindroseServerManager.Core/Services/ServerProcessService.cs, src/WindroseServerManager.Core/Services/IServerProcessService.cs, tests/WindroseServerManager.Core.Tests/Services/ServerProcessServiceLauncherTests.cs</files>
  <read_first>
    - src/WindroseServerManager.App/App.axaml.cs (lines around 70-95 — DI block)
    - src/WindroseServerManager.Core/Services/ServerProcessService.cs §StartAsync (lines 49-120) and §ValidateCanStart (lines 38-47)
    - src/WindroseServerManager.Core/Services/IServerProcessService.cs
    - src/WindroseServerManager.Core/Services/WindrosePlusService.cs (Plan 02)
    - src/WindroseServerManager.Core/Models/ServerInstallInfo.cs
    - tests/WindroseServerManager.Core.Tests/Fixtures/TempServerFixture.cs
  </read_first>
  <action>
    1. **DI registration** — edit `src/WindroseServerManager.App/App.axaml.cs` in the `ConfigureServices`-equivalent block (near line 70-90 where other `AddSingleton` calls are). Add ONE line immediately after the `AddSingleton<ISteamCmdService, SteamCmdService>();` line:
       ```csharp
       s.AddSingleton<IWindrosePlusService, WindrosePlusService>();
       ```

    2. **Extend `ServerProcessService`** to use `IWindrosePlusService.ResolveLauncher`:
       - Add `IWindrosePlusService _windrosePlus;` field.
       - Change constructor signature to: `public ServerProcessService(ILogger<ServerProcessService> logger, IAppSettingsService settings, IServerEventLog events, IWindrosePlusService windrosePlus)`.
       - Store the injected service.
       - Inside `StartAsync`, REPLACE the two lines:
         ```csharp
         var dir = _settings.Current.ServerInstallDir;
         var exe = ServerInstallService.FindServerBinary(dir)!;
         var args = BuildLaunchArgs(_settings.Current);
         ```
         with:
         ```csharp
         var dir = _settings.Current.ServerInstallDir;
         var info = BuildInstallInfo(dir);
         var (exe, wplusArgs) = _windrosePlus.ResolveLauncher(dir, info);
         var args = CombineArgs(BuildLaunchArgs(_settings.Current), wplusArgs);
         ```
         Add two private helpers:
         ```csharp
         private ServerInstallInfo BuildInstallInfo(string dir)
         {
             var s = _settings.Current;
             var key = string.IsNullOrWhiteSpace(dir) ? "" : Path.GetFullPath(dir);
             var active = s.WindrosePlusActiveByServer.TryGetValue(key, out var a) && a;
             var tag    = s.WindrosePlusVersionByServer.TryGetValue(key, out var t) ? t : null;
             return new ServerInstallInfo(
                 IsInstalled: !string.IsNullOrWhiteSpace(dir) && Directory.Exists(dir),
                 InstallDir: dir,
                 BuildId: null,
                 SizeBytes: 0,
                 LastUpdatedUtc: null,
                 WindrosePlusActive: active,
                 WindrosePlusVersionTag: tag);
         }

         private static string CombineArgs(string a, string b) =>
             string.IsNullOrWhiteSpace(b) ? a : (string.IsNullOrWhiteSpace(a) ? b : a + " " + b);
         ```
       - Update `ValidateCanStart` similarly: replace `var exe = ServerInstallService.FindServerBinary(dir);` with `var info = BuildInstallInfo(dir); var (exe, _) = _windrosePlus.ResolveLauncher(dir, info);` and wrap the call in `try { ... } catch (FileNotFoundException) { return $"Server-Binary nicht gefunden in {dir}..."; }`.

    3. **Create integration test** `tests/WindroseServerManager.Core.Tests/Services/ServerProcessServiceLauncherTests.cs`:
       ```csharp
       // [Fact] Start_UsesBat_WhenWindrosePlusActive_AndBatExists
       //   - TempServerFixture: create StartWindrosePlusServer.bat at server root
       //   - AppSettings.WindrosePlusActiveByServer[fullPath] = true
       //   - Build a ServerProcessService with a real WindrosePlusService (cacheDir=fixture.CacheDir, handler=FakeGithubReleaseServer just for instantiation — never invoked)
       //   - Call ValidateCanStart(); MUST return null (no error)
       //   - Reflection or test hook: assert that BuildInstallInfo produces an info with WindrosePlusActive=true and ResolveLauncher(dir, info).ExePath ends with "StartWindrosePlusServer.bat"
       //
       // [Fact] Start_UsesExe_WhenOptedOut
       //   - AppSettings empty → info.WindrosePlusActive=false → ResolveLauncher returns the .exe
       //
       // [Fact] Start_FallsBackToExe_WhenActiveButBatMissing
       //   - Flag=true but no .bat → ResolveLauncher returns .exe AND test-logger records a Warning mentioning "StartWindrosePlusServer.bat"
       ```
       Use the existing `IWindrosePlusService` tests' TestLogger pattern. For the real `WindrosePlusService` instantiation reuse the `FakeHttpClientFactory` from Plan 01 (no API call happens in ResolveLauncher, so any handler works).

       Because `ResolveLauncher` is a pure function of inputs (not StartAsync), these tests call `ResolveLauncher` directly on the service — NOT through `ServerProcessService.StartAsync` (which would spawn a real process). The `ServerProcessService` constructor wiring is validated by the existing build step.

    4. **Update any existing callers / tests that construct `ServerProcessService`** — search with `grep -rn "new ServerProcessService(" src tests` — add the `IWindrosePlusService` argument. If none exist outside DI, skip.

    Coding standards:
    - `using` for the new type: `using WindroseServerManager.Core.Services;` in `App.axaml.cs` (likely already present).
    - Structured logging in any new log lines.
  </action>
  <verify>
    <automated>dotnet build WindroseServerManager.sln --nologo -v minimal 2>&amp;1 | tail -20 &amp;&amp; dotnet test tests/WindroseServerManager.Core.Tests --filter "FullyQualifiedName~ServerProcessServiceLauncherTests|FullyQualifiedName~WindrosePlus" --nologo -v minimal</automated>
  </verify>
  <validation>
    - Build succeeds (solution-wide)
    - 3 new ServerProcessServiceLauncherTests pass
    - All 13 WindrosePlusServiceTests still pass
    - AppSettings round-trip test still passes
  </validation>
  <done>
    - `grep -c "AddSingleton<IWindrosePlusService, WindrosePlusService>" src/WindroseServerManager.App/App.axaml.cs` returns 1
    - `grep -c "_windrosePlus" src/WindroseServerManager.Core/Services/ServerProcessService.cs` returns at least 2 (field + usage)
    - `ServerProcessServiceLauncherTests.cs` file exists with 3 `[Fact]` methods
    - All mentioned tests green
  </done>
  <acceptance_criteria>
    - `grep -c "IWindrosePlusService windrosePlus" src/WindroseServerManager.Core/Services/ServerProcessService.cs` returns 1 (constructor param)
    - `grep -c "_windrosePlus.ResolveLauncher" src/WindroseServerManager.Core/Services/ServerProcessService.cs` returns at least 2 (StartAsync + ValidateCanStart)
    - `grep -c "AddSingleton<IWindrosePlusService" src/WindroseServerManager.App/App.axaml.cs` returns 1
    - `grep -c "\[Fact\]" tests/WindroseServerManager.Core.Tests/Services/ServerProcessServiceLauncherTests.cs` returns 3
    - `dotnet test tests/WindroseServerManager.Core.Tests --nologo 2>&1 | grep -E "Failed"` shows `Failed: 0`
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Embed WindrosePlus LICENSE + update About dialog with Third-Party Licenses section + localization strings</name>
  <files>src/WindroseServerManager.App/Resources/Licenses/WindrosePlus-LICENSE.txt, src/WindroseServerManager.App/WindroseServerManager.App.csproj, src/WindroseServerManager.App/Resources/Strings.de.axaml, src/WindroseServerManager.App/Resources/Strings.en.axaml, src/WindroseServerManager.App/Views/Dialogs/AboutDialog.axaml, src/WindroseServerManager.App/Views/Dialogs/AboutDialog.axaml.cs</files>
  <read_first>
    - src/WindroseServerManager.App/Views/Dialogs/AboutDialog.axaml (current layout)
    - src/WindroseServerManager.App/Views/Dialogs/AboutDialog.axaml.cs (OnLinkClick handler)
    - src/WindroseServerManager.App/WindroseServerManager.App.csproj (for AvaloniaResource entry pattern)
    - src/WindroseServerManager.App/Services/LocalizationService.cs (how keys are loaded)
    - src/WindroseServerManager.App/Resources/Strings.de.axaml (existing key format)
    - src/WindroseServerManager.App/Resources/Strings.en.axaml
    - .planning/phases/08-windroseplus-bootstrap/08-CONTEXT.md §"About-Dialog License Exposure"
  </read_first>
  <action>
    1. **Create `src/WindroseServerManager.App/Resources/Licenses/WindrosePlus-LICENSE.txt`** — download and paste the exact MIT LICENSE text from `https://github.com/HumanGenome/WindrosePlus/blob/main/LICENSE`. MUST start with the line `MIT License` and contain `Copyright (c)` and `HumanGenome` (or the exact upstream copyright holder). Do NOT alter the text. Commit the raw file.

    2. **Register as Avalonia resource** — edit `src/WindroseServerManager.App/WindroseServerManager.App.csproj`. Find the existing `<ItemGroup>` that contains `<AvaloniaResource Include="Resources\**\*.axaml" />` or similar. Add (or extend) an item group so the LICENSE file is embedded:
       ```xml
       <ItemGroup>
         <AvaloniaResource Include="Resources\Licenses\WindrosePlus-LICENSE.txt" />
       </ItemGroup>
       ```
       The runtime URI will be `avares://WindroseServerManager.App/Resources/Licenses/WindrosePlus-LICENSE.txt`.

    3. **Add localization keys** — append to `Strings.de.axaml` inside the existing `<ResourceDictionary>`:
       ```xml
       <sys:String x:Key="About.ThirdPartyLicenses.Heading">Lizenzen von Drittanbietern</sys:String>
       <sys:String x:Key="About.ThirdPartyLicenses.WindrosePlus.Intro">WindrosePlus (MIT-Lizenz) — © HumanGenome. Bündelt UE4SS und den WindrosePlus-Server-Mod.</sys:String>
       <sys:String x:Key="About.WindrosePlus.RepoLink">WindrosePlus auf GitHub</sys:String>
       <sys:String x:Key="About.WindrosePlus.ShowLicense">Lizenztext anzeigen</sys:String>
       <sys:String x:Key="Warning.WindrosePlusBatMissing">WindrosePlus ist aktiviert, aber StartWindrosePlusServer.bat fehlt. Starte stattdessen WindroseServer.exe.</sys:String>
       <sys:String x:Key="Error.WindrosePlusOfflineFirstInstall">WindrosePlus kann nicht installiert werden: GitHub nicht erreichbar und kein Cache vorhanden. Bitte Internetverbindung prüfen und erneut versuchen.</sys:String>
       ```
       Append the English equivalents to `Strings.en.axaml`:
       ```xml
       <sys:String x:Key="About.ThirdPartyLicenses.Heading">Third-Party Licenses</sys:String>
       <sys:String x:Key="About.ThirdPartyLicenses.WindrosePlus.Intro">WindrosePlus (MIT License) — © HumanGenome. Bundles UE4SS and the WindrosePlus server mod.</sys:String>
       <sys:String x:Key="About.WindrosePlus.RepoLink">WindrosePlus on GitHub</sys:String>
       <sys:String x:Key="About.WindrosePlus.ShowLicense">Show license text</sys:String>
       <sys:String x:Key="Warning.WindrosePlusBatMissing">WindrosePlus is active but StartWindrosePlusServer.bat is missing. Launching WindroseServer.exe instead.</sys:String>
       <sys:String x:Key="Error.WindrosePlusOfflineFirstInstall">WindrosePlus cannot be installed: GitHub is unreachable and no cache is available. Please check your internet connection and try again.</sys:String>
       ```

    4. **Update `AboutDialog.axaml`** — insert a new section between the existing Row 1 `StackPanel Grid.Row="1"` content (after `About.Disclaimer` TextBlock) and the footer button row. Grow the Grid's RowDefinitions from `Auto,*,Auto` to `Auto,Auto,Auto,Auto` and shift footer to `Grid.Row="3"`. New `Grid.Row="2"` content:
       ```xml
       <StackPanel Grid.Row="2" Spacing="6" Margin="0,16,0,0">
           <TextBlock Classes="section-header" Text="{DynamicResource About.ThirdPartyLicenses.Heading}" />
           <TextBlock Classes="body" TextWrapping="Wrap"
                      Text="{DynamicResource About.ThirdPartyLicenses.WindrosePlus.Intro}" />
           <StackPanel Orientation="Horizontal" Spacing="8">
               <Button Classes="subtle"
                       Content="{DynamicResource About.WindrosePlus.RepoLink}"
                       Tag="https://github.com/HumanGenome/WindrosePlus"
                       Click="OnLinkClick" />
               <Button Classes="subtle"
                       Content="{DynamicResource About.WindrosePlus.ShowLicense}"
                       Click="OnShowWindrosePlusLicenseClick" />
           </StackPanel>
           <Border x:Name="WindrosePlusLicenseBox"
                   IsVisible="False"
                   CornerRadius="4"
                   Padding="12"
                   Margin="0,4,0,0"
                   MaxHeight="220"
                   Background="{DynamicResource BrandNavySurfaceAltBrush}"
                   BorderBrush="{DynamicResource BrandNavyBorderBrush}"
                   BorderThickness="1">
               <ScrollViewer VerticalScrollBarVisibility="Auto">
                   <TextBlock x:Name="WindrosePlusLicenseText"
                              FontFamily="Consolas, 'Courier New', monospace"
                              FontSize="11"
                              TextWrapping="Wrap" />
               </ScrollViewer>
           </Border>
       </StackPanel>
       ```
       Use an existing dark-surface brush for the license box — if `BrandNavySurfaceAltBrush` does not exist, use `BrandNavySurfaceBrush` (the dialog background) or the closest alt. Grep for existing brushes: `grep -rn "BrandNavySurface" src/WindroseServerManager.App/Resources/`.

    5. **Update `AboutDialog.axaml.cs`** — add a handler:
       ```csharp
       private async void OnShowWindrosePlusLicenseClick(object? sender, Avalonia.Interactivity.RoutedEventArgs e)
       {
           try
           {
               if (WindrosePlusLicenseText.Text is { Length: > 0 })
               {
                   WindrosePlusLicenseBox.IsVisible = !WindrosePlusLicenseBox.IsVisible;
                   return;
               }
               var uri = new Uri("avares://WindroseServerManager.App/Resources/Licenses/WindrosePlus-LICENSE.txt");
               await using var stream = Avalonia.Platform.AssetLoader.Open(uri);
               using var reader = new StreamReader(stream, System.Text.Encoding.UTF8);
               WindrosePlusLicenseText.Text = await reader.ReadToEndAsync();
               WindrosePlusLicenseBox.IsVisible = true;
           }
           catch (Exception ex)
           {
               // Fallback: show a short apology in the text block rather than crashing the dialog.
               WindrosePlusLicenseText.Text = "Failed to load license: " + ex.Message;
               WindrosePlusLicenseBox.IsVisible = true;
           }
       }
       ```
       Add `using System.IO;` if not present.
  </action>
  <verify>
    <automated>dotnet build src/WindroseServerManager.App/WindroseServerManager.App.csproj --nologo -v minimal</automated>
  </verify>
  <validation>
    - App project compiles with 0 errors, 0 new warnings
    - `grep -l "MIT License" src/WindroseServerManager.App/Resources/Licenses/WindrosePlus-LICENSE.txt` matches
    - New localization keys exist in both language files
    - About dialog XAML references all new keys
  </validation>
  <done>
    - `WindrosePlus-LICENSE.txt` exists under `Resources/Licenses/` starting with "MIT License"
    - `.csproj` includes the file as `<AvaloniaResource>`
    - Both `Strings.de.axaml` and `Strings.en.axaml` contain the 6 new keys
    - `AboutDialog.axaml` has a `<TextBlock>` bound to `About.ThirdPartyLicenses.Heading` and a button with `Tag="https://github.com/HumanGenome/WindrosePlus"`
    - `AboutDialog.axaml.cs` has `OnShowWindrosePlusLicenseClick` method
    - App builds
  </done>
  <acceptance_criteria>
    - `grep -c "MIT License" src/WindroseServerManager.App/Resources/Licenses/WindrosePlus-LICENSE.txt` returns at least 1
    - `grep -c "AvaloniaResource Include=\"Resources\\\\Licenses\\\\WindrosePlus-LICENSE.txt\"" src/WindroseServerManager.App/WindroseServerManager.App.csproj` returns 1
    - `grep -c "About.ThirdPartyLicenses.Heading" src/WindroseServerManager.App/Resources/Strings.de.axaml` returns 1
    - `grep -c "About.ThirdPartyLicenses.Heading" src/WindroseServerManager.App/Resources/Strings.en.axaml` returns 1
    - `grep -c "https://github.com/HumanGenome/WindrosePlus" src/WindroseServerManager.App/Views/Dialogs/AboutDialog.axaml` returns 1
    - `grep -c "OnShowWindrosePlusLicenseClick" src/WindroseServerManager.App/Views/Dialogs/AboutDialog.axaml.cs` returns 1
    - `grep -c "WindrosePlus" src/WindroseServerManager.App/Views/Dialogs/AboutDialog.axaml` returns at least 3 (section intro, repo button, license button) — proves the name appears verbatim, never rebranded
    - App build reports 0 errors
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking" status="approved">
  <name>Task 3: Human smoke test — About dialog and launcher switch [APPROVED 2026-04-19]</name>
  <files>(none — verification step only; exercises artifacts from Task 1 and Task 2)</files>
  <what-built>
    Task 1: DI registration + ServerProcessService launcher indirection (automated tests green).
    Task 2: About dialog "Third-Party Licenses" section showing WindrosePlus MIT text + GitHub link; embedded LICENSE resource; DE+EN strings.
  </what-built>
  <action>
    Pause execution and hand control to the user. The user performs the manual verification steps in `<how-to-verify>` and signals resume.
  </action>
  <how-to-verify>
    1. Run `dotnet run --project src/WindroseServerManager.App`
    2. Open Settings → Info/About dialog
    3. Verify the "Lizenzen von Drittanbietern" (DE) / "Third-Party Licenses" (EN) section is visible
    4. Verify the text mentions "WindrosePlus" verbatim and "HumanGenome"
    5. Click "WindrosePlus auf GitHub" / "WindrosePlus on GitHub" — confirm browser opens to https://github.com/HumanGenome/WindrosePlus
    6. Click "Lizenztext anzeigen" / "Show license text" — confirm license box opens and shows "MIT License" at the top
    7. Switch language to English in Settings — confirm labels update
    8. Close dialog without errors
    9. (Optional) If a real Windrose server is installed, toggle `AppSettings.WindrosePlusActiveByServer["<path>"]` to true via editing the JSON settings file, drop a dummy `StartWindrosePlusServer.bat` at the server root, restart app, click Start — confirm log line shows the .bat path (not the .exe).
  </how-to-verify>
  <verify>Manual smoke per the 8 numbered steps above. Automated coverage for WPLUS-03 About UI is intentionally out of scope (see 08-VALIDATION.md §"Manual-Only Verifications").</verify>
  <validation>
    Automated coverage for WPLUS-03 (About UI) is intentionally out of scope (see 08-VALIDATION.md §"Manual-Only Verifications"). This checkpoint is the gate.
  </validation>
  <done>User types "approved" after all 8 steps pass. Any issue found is filed as a blocker and this plan re-opens.</done>
  <resume-signal>Type "approved" after all 8 steps pass, or list issues found.</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build WindroseServerManager.sln --nologo` — 0 errors
- `dotnet test tests/WindroseServerManager.Core.Tests --nologo` — 0 failed
- Human checkpoint signed off
- 08-VALIDATION.md Per-Task Verification Map populated for Plan 03 tasks
</verification>

<success_criteria>
- WPLUS-03: About dialog shows WindrosePlus MIT text + HumanGenome link; LICENSE embedded as resource; name "WindrosePlus" appears verbatim in the UI
- WPLUS-04: ServerProcessService queries IWindrosePlusService.ResolveLauncher; ServerProcessServiceLauncherTests all green
- DI graph resolves without exceptions on app start
- Bilingual (DE + EN) strings for every user-facing message added in this phase
</success_criteria>

<output>
After completion, create `.planning/phases/08-windroseplus-bootstrap/08-03-SUMMARY.md` documenting:
- Final DI registration location (line number in App.axaml.cs)
- Exact brush names used in AboutDialog if `BrandNavySurfaceAltBrush` did not exist
- Any deviations from the localization key names (downstream phases consume these)
- Human checkpoint sign-off timestamp
</output>
